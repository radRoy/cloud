#!/bin/bash

# builds on test_dirname.sh or so
# builds on prompt_yn.sh or so

cd ~/$(dirname $0)

# .. test the prompt_yn.sh function by calling it externally from this function here, in another folder